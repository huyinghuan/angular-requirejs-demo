define(['app', 'd/harddisk-table'],
  (app)->
    app.controller('DetailController',
      [
        '$scope'
        '$state'
        '$log'
        ($scope, $state, $log)->
          $log
      ]
    )
)
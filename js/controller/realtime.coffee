define(['app', 'd/server-list-table'],
  (app)->
    app.controller('RealtimeController',
      [
        '$scope'
        '$state'
        '$log'
        ($scope, $state, $log)->
          $log
      ]
    )
)